varying vec3 color = vec3(0.0, 0.0, 0.0);

float
IntersectUnitSphere(vec3 rayOrig, vec3 rayDir)
{
  return 0.0;
}

void
SamplePixel(varying vec2 uv)
{
  color = vec3(uv.xy, 1.0);
}

vec4
EncodePixel()
{
  return vec4(color.xyz, 1.0);
}
